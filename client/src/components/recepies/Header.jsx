export function Header(props) {
    return (
        // <h1>header</h1>
        <header>
            <h1>Assembly: Endgame</h1>
            <p>Guess the word within {props.attempts} attempts to keep the
                programming world safe from Assembly!</p>
        </header>
    )
}